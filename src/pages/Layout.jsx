// put in header - own component
// put in side panel - own component
// style in layout
export default function Layout() {
  return (
    <div>
      <header>
        <span>TEST</span>
      </header>
    </div>
  );
}
